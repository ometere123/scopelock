import { SubmitReport } from "@/components/submit-report";
export default async function Submit({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="ledger-section"><p className="eyebrow">SUBMIT REPORT / PROGRAM {id}</p><h1>Bind public evidence.</h1><p>Similarity is a search signal. It never decides whether your report is a duplicate. ScopeLock returns final status only from chain state after consensus.</p><SubmitReport programId={id}/></main>; }
