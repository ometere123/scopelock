export default async function Disclosure({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="rail-section"><div><p className="eyebrow">DISCLOSURE DOSSIER / SL-{id}</p><h1>Chain record required.</h1></div><ol className="rail"><li><time>01</time><b>SUBMISSION BOUND</b><small>Load the on-chain report to show its evidence and transaction chronology.</small></li><li><time>02</time><b>CONSENSUS</b><small>Verdict and settlement are authoritative only when read from ScopeLock.</small></li></ol></main>; }
